{
    "configurations": [
        {
            "type": "gdb",
            "request": "attach",
            "name": "Attach to gdbserver : usrprog",
            //"executable": "${workspaceRoot}/threads/build/kernel.o",
            "executable": "${workspaceRoot}/userprog/build/kernel.o",
            "target": "localhost:1234",
            "remote": true,
            "cwd": "${workspaceRoot}",
            "valuesFormatting": "parseText"
        },
        {
            "type": "gdb",
            "request": "attach",
            "name": "Attach to gdbserver : threads",
            "executable": "${workspaceRoot}/threads/build/kernel.o",
            // "executable": "${workspaceRoot}/usrprog/build/kernel.o",
            "target": "localhost:1234",
            "remote": true,
            "cwd": "${workspaceRoot}",
            "valuesFormatting": "parseText"
        }
    ]
}